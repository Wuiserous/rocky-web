export const metadata = {
  title: "Terms of Service | Rocky Desktop Pet",
  description:
    "Terms of service for using the Rocky Desktop Pet website, downloads, and desktop application.",
};

export default function TermsLayout({ children }) {
  return children;
}
